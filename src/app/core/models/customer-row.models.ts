export interface CustomerRow {
    id: string;
    firstName: string;
    lastName: string;
    email: string;
    gender: string;
    address: string;
    image: string;
    phoneNumber: string;
    totalBalance: number;
}